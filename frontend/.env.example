# Next.js proxies /api/* to the backend URL below (server-side, not exposed to browser)
BACKEND_URL=http://localhost:4000

# Used only if you need to call backend directly from browser (not needed with proxy)
NEXT_PUBLIC_API_URL=http://localhost:4000
